import {Page} from "../common/Types";
import TracksModel from "../model/TracksModel"
//to be used in future updates
class TrackViewModel {
    model = new TracksModel()
    fetchTopTracks(page: Page){

    }
}

export default TrackViewModel